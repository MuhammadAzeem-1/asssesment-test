-> FrontEnd

------ React ------

--Main dependencies
React Router Dom
react redux
React icons
axios

-- Run FrontEnd

Navigate to assesment directory in terminal then

use " cd client" to go in client directory.

Run "npm install" to install the required dependencies.

( No need to create the .env file)

-> Backend

------ Node.js ------

Frameword : Express
Database: MongoDb

--Main dependencies
express
express-async-handler
mongoose
dotenv
node-corn

-- Run Backend

Navigate to assesment directory in terminal then

use " cd server" to go in client directory.

Run "npm install" to install the required dependencies.

For database connection, create a .env file in root directory and enter following info...

<!-- CONNECTION_URL= mongodb+srv://assessment:assesment123@azeemcluster.gitqx1h.mongodb.net/?retryWrites=true&w=majority
PORT=5000 -->

( Remove the comments)

At last, run command like "node index.js" or "node --watch index.js"
